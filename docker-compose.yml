services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: coding_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5434:5432"  # Usar porta 5434 para não conflitar com ysh-postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: